name: weather_app
description: A simple Flutter weather app.
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  intl: ^0.18.1
  shared_preferences: ^2.2.3
  geolocator: ^10.1.0
  cupertino_icons: ^1.0.8
  share_plus: ^11.1.0
  google_maps_flutter: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
